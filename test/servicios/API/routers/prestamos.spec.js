var assert = require('assert');

describe("Deberian funcionar todas las rutas de prestamos", async () => {
    it("Deberia poderse eliminar un prestamo", async () => {
        assert.equal(1,1)
    })
})